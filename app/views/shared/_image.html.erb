<% if image.present? && (!image.respond_to?(:persisted?) || image.persisted?) %>
  <%= image_tag image.url, loading: "lazy" %>
<% else %>
  <%= image_tag "FotoPorDefecto.png", loading: "lazy" %>
<% end %>
